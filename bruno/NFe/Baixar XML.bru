meta {
  name: Baixar XML
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/nfe/{{notaFiscalId}}/xml
  body: none
  auth: none
}

headers {
  x-api-key: {{apiKey}}
}

docs {
  XML autorizado (procNFe) da NF-e.
}

tests {
  test("deve retornar o XML (200)", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.include("<");
  });
}
